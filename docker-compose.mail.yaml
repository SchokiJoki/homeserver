services:
  # 📮 Lokaler Postfix-Relay (System-SMTP)
  smtp-local:
    image: mwader/postfix-relay:latest  # was 1.2.2@sha256:52e4c937f7e925b055315fddfad90e2154e38fec83f9d9cf1396eca09dc4f16b
    container_name: smtp-local
    restart: unless-stopped
    environment:
      - POSTFIX_relayhost=mailpit:1025
      - MYNETWORKS=127.0.0.0/8 172.17.0.0/16 172.18.0.0/16 192.168.178.0/24
      - MYHOSTNAME=relay.homeserver.home
      - SMTP_USE_TLS=no
      - SMTP_TLS_SECURITY_LEVEL=none
      - TZ=Europe/Berlin
      # Wenn du später ein externes Relay willst (Mailbox.org, Mailjet etc.)
      # kannst du hier RELAYHOST, RELAY_USER, RELAY_PASSWORD ergänzen
    volumes:
      - ./maildata:/var/mail
    networks:
      - proxy
    ports:
      - "25:25"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.smtp-local.rule=Host(`smtp.home`)"
      - "traefik.http.routers.smtp-local.entrypoints=websecure"
      - "traefik.http.routers.smtp-local.tls=true"
      - "traefik.http.services.smtp-local.loadbalancer.server.port=25"
      - "com.centurylinklabs.watchtower.enable=true"

  # 📬 Mailpit (zeigt alle eingehenden Mails im Browser)
  mailpit:
    image: axllent/mailpit:latest  # was v1.27@sha256:798783f7e4bff6d15795e7799221187c263763e434cf3e84f0546e8f4be27962
    container_name: mailpit
    restart: unless-stopped
    environment:
      - MP_UI_BIND_ADDR=0.0.0.0:8025
      - MP_SMTP_BIND_ADDR=0.0.0.0:1025
      - TZ=Europe/Berlin
    networks:
      - proxy
    depends_on:
      - smtp-local
    ports:
      - "1025:1025"  # SMTP-Eingang (von Postfix)
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.mailpit.rule=Host(`mail.home`)"
      - "traefik.http.routers.mailpit.entrypoints=websecure"
      - "traefik.http.routers.mailpit.tls=true"
      - "traefik.http.services.mailpit.loadbalancer.server.port=8025"
      - "com.centurylinklabs.watchtower.enable=true"

networks:
  proxy:
    external: true
    name: proxy
